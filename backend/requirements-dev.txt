# requirements-dev.txt — pinned development and test dependencies
#
# Purpose: reproducible CI builds. Every pip install in GitHub Actions
# and locally installs exactly these versions — not "whatever is latest".
#
# To update: pip install --upgrade pytest boto3 pytest-cov && pip freeze
# Then update the versions below and commit.
#
# boto3 is listed here for local testing only.
# It is NOT bundled into the Lambda deployment package (provided by runtime).

boto3==1.38.0
botocore==1.38.0
pytest==8.3.5
pytest-cov==6.1.0
