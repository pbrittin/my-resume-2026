# boto3 is provided by the Lambda Python 3.12 runtime.
# Do not add it here — bundling it bloats the deployment package unnecessarily.
#
# Add any additional runtime dependencies below if needed in future.
